# LocalConvert — Claude Code Context
# =====================================
# Use this as context when working with Claude Code in VS Code
# Last updated: March 28, 2026
# =====================================


## PROJECT OVERVIEW

LocalConvert is a privacy-first file converter forked from VERT.sh (AGPL-3.0). All conversions run locally in the browser via WASM — files never touch a server. Live at https://localconvert.app.

**Tech Stack:**
- Frontend: SvelteKit + TypeScript + Tailwind CSS 3.4
- Hosting: Cloudflare Pages (auto-deploy on git push)
- Image engine: @imagemagick/magick-wasm
- Audio/Video (CPU): @ffmpeg/core@0.12.10 (WASM, CDN-loaded)
- Video (GPU): Mediabunny + web-demuxer (WebCodecs API)
- Documents: Pandoc WASM (hosted on R2, 50MB)
- PDF tools: pdf-lib (MIT) + pdfjs-dist 5.x (Apache 2.0) + pdf-lib-plus-encrypt (MIT, for password protection)
- Presentations: pptxgenjs (MIT, for PDF → PPT)
- WASM hosting: Cloudflare R2 bucket `localconvert-assets`
- State: Svelte 5 runes ($state, $derived, $derived.by, $effect, $props)
- i18n: Paraglide-JS — 16 locales (ar, ba, de, el, en, es, fr, hr, id, it, ja, ko, pt-BR, tr, zh-Hans, zh-Hant)
- PWA: manifest.json + sw.js + beforeinstallprompt capture
- CSS: SCSS with CSS variables for theming (light/dark mode)
- Styling: Tailwind + custom CSS variables in app.scss + `dynadark:` variant for dark mode
- Fonts: Azeret Mono (display/headings), Lexend (body) via @fontsource
- Icons: Lucide-Svelte

**Repository:** github.com/Sp3llC0d3/localconvert
**Domain:** localconvert.app (Cloudflare)
**Deploy:** `git push origin main` → Cloudflare auto-builds
**Build command:** `npm install --legacy-peer-deps && npm run build`
**Local dev:** `npm run dev` → http://localhost:5174/
**Sitemap:** 225 URLs — generated by `scripts/generate-sitemap.js`


## TOOLS OVERVIEW

### Main File Converter (`/convert`)
- Images (ImageMagick): 60+ formats including jpg, png, webp, avif, gif, svg, jxl, heic, ico, bmp, tiff, psd, raw camera formats
- Audio (FFmpeg): mp3, wav, flac, aac, ogg, opus, m4a, wma, aiff + 15 more
- Video (FFmpeg): mp4, mkv, webm, avi, mov, wmv, flv + 12 more
- Documents (Pandoc): docx, doc, html, md, epub, odt, rtf, csv, tsv, rst, json, docbook
- Per-file controls: quality slider, resize, bitrate, trim

### PDF Tools (`/pdf-tools`) — 18 tools
| Tool | Route | Logic | Interactive |
|------|-------|-------|-------------|
| Merge PDF | `/merge` | merge.ts | File list |
| Split PDF | `/split` | split.ts | Thumbnail selection |
| Rotate Pages | `/rotate` | rotate.ts | Visual rotation indicators |
| Organize Pages | `/organize` | organize.ts | Arrow buttons + thumbnails |
| Images → PDF | `/images-to-pdf` | images-to-pdf.ts | Thumbnail reorder |
| PDF → Images | `/pdf-to-images` | pdf-to-images.ts | Format/quality options |
| PDF → PowerPoint | `/pdf-to-ppt` | pdf-to-ppt.ts | Progress bar |
| Compress PDF | `/compress` | compress.ts | Before/after comparison |
| Add Watermark | `/watermark` | watermark.ts | **Live canvas preview** |
| Add Page Numbers | `/page-numbers` | page-numbers.ts | **Live canvas preview** |
| Edit Metadata | `/metadata` | metadata.ts | Form fields |
| Crop PDF | `/crop` | crop.ts | **Draggable edge handles** |
| Sign PDF | `/sign` | sign.ts | **Click-to-place + drag** |
| Edit PDF | `/edit` | edit.ts | **Click-to-place + undo/redo** |
| Password Protect | `/password` | password.ts | Permissions UI |
| Unlock PDF | `/unlock` | unlock.ts | Password input |
| Grayscale PDF | `/grayscale` | grayscale.ts | Before/after comparison |
| PDF → Text | `/pdf-to-text` | pdf-to-text.ts | Progress bar + copy |

### Image Tools (`/image-tools`) — 10 tools
| Tool | Route | Logic | Interactive |
|------|-------|-------|-------------|
| Rotate Image | `/rotate` | image/rotate.ts | **Live canvas preview** |
| Crop Image | `/crop` | image/crop.ts | **Interactive handles + circle crop** |
| Resize Image | `/resize` | image/resize.ts | Presets + custom dimensions |
| Compress Image | `/compress` | image/compress.ts | Quality slider + before/after |
| Convert Image | `/convert` | image/convert.ts | Format selection |
| Watermark Image | `/watermark` | image/watermark.ts | **Live canvas preview** |
| Meme Generator | `/meme` | image/meme.ts | **Live canvas preview** |
| Blur Region | `/blur` | image/blur.ts | **Interactive region select** |
| Color Picker | `/color-picker` | — | Eyedropper + HEX/RGB/HSL copy |
| Filters | `/filters` | image/filters.ts | **Live canvas preview** |

### Dev Tools (`/dev-tools`) — 8 tools
| Tool | Route | Interactive |
|------|-------|-------------|
| JSON Formatter | `/json` | Live validation + format/minify |
| Hash Generator | `/hash` | SHA-256/SHA-1/SHA-512, text or file |
| Base64 Encode/Decode | `/base64` | Encode/decode with Unicode support |
| Markdown Preview | `/markdown` | Split-pane live preview |
| Text Diff | `/diff` | LCS-based line diff |
| Word Counter | `/word-count` | Live stats (words, chars, reading time) |
| URL Encode/Decode | `/url-encode` | encodeURIComponent |
| CSS Minifier | `/css-minify` | Regex-based minification |

### Interaction Patterns
- **Live canvas preview** — rAF loop redraws on every setting change, base image cached as ImageData
- **Click-to-place** — Ghost element follows cursor, click places, drag repositions (HTML overlays, not canvas)
- **Undo/redo** — Generic history system (`src/lib/util/history.svelte.ts`), Ctrl+Z/Ctrl+Shift+Z
- **Keyboard shortcuts** — Delete, Escape, Arrow keys (1pt nudge, Shift=10pt) on PDF Edit
- **Draggable handles** — PDF Crop has 4 edge handles synced with number inputs
- **PDF preview utility** — `src/lib/pdf/preview.ts` with `loadPdfDocument()` + `renderDocPageToCanvas()` for single-parse, multi-page preview


## KEY FILE PATHS

### Core Files
- `src/lib/css/app.scss` — All CSS variables (theme colors, gradients)
- `src/routes/+page.svelte` — Home page
- `src/routes/+layout.svelte` — Root layout (meta tags, skip link, main wrapper)
- `src/routes/convert/+page.svelte` — Converter page
- `tailwind.config.ts` — Tailwind custom colors/plugins

### PDF Tools
- `src/lib/pdf/` — merge, split, rotate, organize, compress, watermark, page-numbers, metadata, crop, sign, edit, password, pdf-to-ppt, images-to-pdf, pdf-to-images, thumbnails, preview, utils
- `src/lib/components/pdf/` — PdfUploader, PdfPageThumbnail
- `src/routes/pdf-tools/` — Landing page + 15 tool routes

### Image Tools
- `src/lib/image/` — rotate, crop, resize, compress, convert, watermark, meme, blur, filters, utils
- `src/lib/components/image/` — ImageUploader (with file type + size validation), BeforeAfter
- `src/routes/image-tools/` — Landing page + 10 tool routes

### Dev Tools
- `src/routes/dev-tools/` — Landing page + 8 tool routes (json, hash, base64, markdown, diff, word-count, url-encode, css-minify)

### Shared Utilities
- `src/lib/util/history.svelte.ts` — Generic undo/redo with Svelte 5 runes
- `src/lib/pdf/preview.ts` — PDF page preview renderer (loadPdfDocument + renderDocPageToCanvas)

### Navbar
- `src/lib/components/layout/Navbar/Base.svelte` — Consolidated navbar with Tools dropdown (PDF + Image + Dev tools), language picker, theme toggle
- `src/lib/components/layout/Navbar/Mobile.svelte` — Fixed bottom nav with spacer for footer
- `src/lib/components/layout/MobileLogo.svelte` — Logo header (visible on all screen sizes)

### Layout & Components
- `src/lib/components/layout/Footer.svelte` — Footer with VERT attribution
- `src/lib/components/layout/Dialogs.svelte` — Dialog with focus trap + aria-modal
- `src/lib/components/layout/Toasts.svelte` — Toast container with aria-live
- `src/lib/components/visual/Tooltip.svelte` — Tooltip with aria-describedby

### i18n
- `messages/*.json` — 16 locale files, all synced with latest keys
- `src/lib/paraglide/messages/` — Compiled (run `npx @inlang/paraglide-js compile`)
- `scripts/add-translations.cjs` — Migration script for adding keys to all locales

### SEO
- `scripts/generate-sitemap.js` — Generates static/sitemap.xml (225 URLs)
- JSON-LD on: home, /[converter], /[from]-to-[to], /image-tools/* pages


## IMPORTANT NOTES

1. **AGPL License** — Must keep VERT attribution in footer. Source link required.
2. **Legacy peer deps** — Always use `npm install --legacy-peer-deps` or builds fail.
3. **Svelte 5 runes** — $state, $derived, $derived.by, $effect, $props (not legacy stores)
4. **Deploy** — Just `git push origin main` — Cloudflare auto-builds and deploys.
5. **Dark mode** — `dynadark:` Tailwind variant, system preference + manual toggle.
6. **UI language** — "select" not "upload", "save" not "download" in all user-facing copy.
7. **Trust badge** — "No server uploads" pill must stay exactly as-is.
8. **i18n workflow** — After adding keys to `messages/en.json`, add to all 15 other locales too, then compile.
9. **Node.js scripts** — Must use `.cjs` extension (project has `"type": "module"`).
10. **PDF preview** — Use `loadPdfDocument()` + `renderDocPageToCanvas()` for efficient multi-page preview. Always call `doc.destroy()` on cleanup.
11. **Live preview pattern** — Cache base image as ImageData, redraw overlay via rAF. No debouncing needed.
12. **Image validation** — ImageUploader validates file type + size (>100MB blocked, >20MB warned).
13. **Scoped CSS** — Svelte scopes styles per-component. Use Tailwind utilities (global) for Panel class props.


## REDESIGN PLAN: "Private Workshop" Transformation

The site evolved from a converter to a 36-tool privacy-first toolkit. The redesign reframes the brand from "converter" to "private workshop" — warm, organized, capable, trustworthy. No architectural rewrites, no new dependencies, no breaking changes.

### Design Principles
- **Metaphor:** "Private Workshop" — well-organized, warm, nothing leaves the room
- **Preserve:** Azeret Mono + Lexend fonts, mint-teal + category colors (blue/purple/red/green), warm cream/charcoal palette, existing CSS variables, Tailwind extensions
- **Evolve:** Hero messaging (converter → toolkit), homepage sections (generic SaaS → tool showcase), tool page headers (inconsistent → branded), footer (bare → brand anchor), discovery (browsing → Cmd+K search)
- **Do NOT add:** Grain texture (fights warm aesthetic), full-page category tints (muddy), navbar dropdown overhaul (already clean), celebration animations (too playful)

### Batch 1: CSS Tokens + Homepage Transformation
**Files:** `src/lib/css/app.scss` + `src/routes/+page.svelte`
**CSS changes:**
- Add `--shadow-panel-hover` (elevated shadow for interactive states)
- Add `@keyframes fadeInUp` (generalized staggered reveal, reusable beyond hero)
- Add `.result-success` class (subtle accent border pulse, 1 cycle, 600ms)
- Add `.category-dot` utility (small colored circle for tool identification)
**Homepage changes:**
- Update hero headline from converter-focused to toolkit-oriented
- Update pills: "GPU Accelerated" → "36+ Tools"
- Update subtitle to mention PDF, image, and dev tools
- Keep uploader EXACTLY as-is (proven conversion UX)
- Replace "How It Works" 3-step with Tool Category Showcase (3 cards: PDF green, Image blue, Dev purple — each with tool count + highlighted tool names + "Explore →" link)
- Reposition Formats section below tool showcase, add "CONVERTER" label
- Update Privacy section copy to reflect full toolkit
- Update FAQ Q3 answer to mention all tool categories
- Update JSON-LD featureList to include image tools + dev tools
**Test:** Load homepage, scroll through all sections, check mobile + dark mode

### Batch 2: Footer Redesign
**File:** `src/lib/components/layout/Footer.svelte`
**Changes:**
- Expand from 3-link single line to structured footer
- Row 1: "LocalConvert" brand + tagline
- Row 2: Tool category links with counts (PDF · 18 | Image · 10 | Dev · 8)
- Row 3: Source Code · Privacy Policy · © 2026
- Keep fixed-bottom desktop / static mobile behavior
- Keep VERT attribution (AGPL requirement)
- Keep gradient fade overlay
- Verify mobile nav spacer still works after height change
**Test:** Visit any page, check footer on desktop + mobile, verify nav spacing

### Batch 3: Tool Landing Pages
**Files:** `src/routes/pdf-tools/+page.svelte`, `src/routes/image-tools/+page.svelte`, `src/routes/dev-tools/+page.svelte`
**Changes:**
- PDF: Enhanced hero with tool count badge, keep existing 3-category grid (Organize/Convert/Optimize), subtle green accent line
- Image: Group flat list into 2 categories ("Edit" + "Create"), blue accent, tool count badge
- Dev: Group flat list into 2 categories ("Format & Transform" + "Analyze"), purple accent, tool count badge
- All: Consistent hero pattern echoing homepage tool showcase cards
**Test:** Visit each landing page, check category grouping, mobile, dark mode

### Batch 4: ToolPageHeader — Pilot
**New file:** `src/lib/components/layout/ToolPageHeader.svelte`
**Pilot files:** `src/routes/pdf-tools/merge/+page.svelte` (simple, 113 lines), `src/routes/image-tools/crop/+page.svelte` (complex, 513 lines, wider layout), `src/routes/dev-tools/diff/+page.svelte` (widest, 60rem)
**Component props:** `category` ("pdf"|"image"|"dev"), `title`, `description`, `backHref`, `backLabel`, `icon`
**Renders:** Back link → category-colored icon box → h1 + description → "Processed locally" pill
**Why these 3:** Merge is simplest, crop is most complex with different max-width, diff is widest layout. If it works on these extremes, it works on everything.
**Test:** Visit these 3 tools, verify header looks correct across all 3 category styles

### Batch 5: ToolPageHeader — PDF Rollout
**Files:** 15 remaining PDF tool pages
**Changes:** Mechanical — import ToolPageHeader, replace duplicated header code (~8 lines removed, ~2 lines added per file), set category="pdf"
**Test:** Spot-check 3-4 PDF tools

### Batch 6: ToolPageHeader — Image + Dev Rollout
**Files:** 9 remaining image tool pages + 7 remaining dev tool pages
**Changes:** Same mechanical migration, blue/purple accents
**Test:** Spot-check 3-4 tools from each category

### Batch 7: Command Palette + Discovery
**New file:** `src/lib/components/layout/CommandPalette.svelte`
**Modified:** `src/routes/+layout.svelte` (import + render + Cmd+K listener), `src/lib/components/layout/Navbar/Base.svelte` (search icon)
**CommandPalette features:**
- Triggered by Cmd+K / Ctrl+K (zero conflicts with existing shortcuts)
- Static array of all 36 tools + converter + pages
- Fuzzy search (simple includes-based, no library)
- Category color dots on each result
- Arrow key navigation + Enter to go + Escape to close
- Recent tools from localStorage when input empty
- Uses existing dialog backdrop pattern (blur + fade)
**Navbar:** Add Search icon next to theme toggle, triggers palette, tooltip: "Search tools (⌘K)"
**Test:** Press Cmd+K on any page, search for tools, navigate, verify Escape closes

### Build Order Rationale
```
Batch 1 (Homepage + CSS)     ← Establishes new visual language + design tokens
Batch 2 (Footer)             ← Completes site shell; isolate height change risk
Batch 3 (Landings)           ← Front doors echo homepage; done after visual language set
Batch 4 (Header pilot)       ← Validate component on 3 extremes before mass migration
Batch 5 (PDF headers)        ← Mechanical; safe after pilot validation
Batch 6 (Image+Dev headers)  ← Mechanical; completes tool page cohesion
Batch 7 (Cmd+K)              ← Cherry on top; depends on final tool list
```
Each batch produces a testable increment. Risk is front-loaded. Pilot prevents rework.

### Risk Mitigation
- Hero copy change: Keep uploader untouched, only change text
- Footer height: Test mobile nav spacer immediately
- "How It Works" removal: Tool showcase serves same trust function
- 36-file migration: Pilot on 3 extremes first (Batch 4)
- i18n: Add English keys, others use fallback until translated


## FUTURE PHASES

### Product & Growth
- Product Hunt launch (copy ready in PRODUCT_HUNT.md, needs screenshots)
- Ad monetization (AdSense, needs 500+ visitors/month first)
- Desktop app (Tauri v2, native FFmpeg, ad-free)
- Mobile app (Tauri v2 or React Native, AdMob)
- Translate tool page strings to 15 non-English locales

### Potential Features
- More image tools: background removal, collage maker
- More PDF tools: form filling, annotation
